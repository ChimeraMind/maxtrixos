# Overall TODO

- [in progress] Rewrite codebase to Golang.
    - check releaser when using rsync, check for /dev device nodes.
    - Consider if possible to implement unshare in code.
      - Let seeders mount /dev, /dev/pts, /sys inside chroot.sh
        now that we have separate mount namespaces via unshare.
        -> no chance of leaking mounts when SIGKILL is sent.
- Add vector command for:
  $ ostree admin pin 1
  $ ostree admin status
- Test and consider migration to systemd-boot (while supporting GRUB).
- Add UKI building to kernel build (make USE flag optional).
- Test bootc & migration to UKI.
- Support Cloud Init (add VM tests).
- Add more VM tests (test flatpak, test snap, test SecureBoot, etc).
  - Add tests for vector
