# We use ntfsplus, and this flag creates /sbin/mount.ntfs that confuses udisks
sys-fs/ntfs3g -mount-ntfs

# This installs a monthly cron job which updates a file in /usr/share.
# And it's bad!
sys-apps/smartmontools -update-drivedb